# ASK v4.0.1 Release Notes

**Release date:** 2026-05-07  
**Release type:** Patch

## Summary

ASK v4.0.1 is a stabilization and release-packaging update after the v4.0.0 major cutover. It promotes a clear version stamp across published package manifests and adds a dedicated release record for downstream consumers.

## Changes

- Bumped root package version from `4.0.0` to `4.0.1`
- Bumped `ask-core` package version from `0.1.0` to `4.0.1` for aligned release lineage
- Added this release note file at `docs/releases/v4.0.1.md`

## Compatibility

- No new runtime commands introduced in this patch
- No new hook contracts introduced in this patch
- Existing v4.0.0 workflows remain valid

## Operational Notes

- Continue using `.githooks`-based enforcement via session adapter wrappers
- Continue using `ask-core/bin/ask.js` as the CLI entrypoint

## Recommended Upgrade Steps

1. Pull the `v4.0.1` tag or branch containing this release.
2. Run your normal runtime verification (`npm test`, hook checks as applicable).
3. Promote through your usual environment rollout sequence.
